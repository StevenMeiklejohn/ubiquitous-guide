(function () {
	'use strict';

	angular.module('ARN')
		.controller('AnalyticsUsersIndex', ['$scope', function($scope) {

			//var profile = $scope.$root.activeProfile;
			//var init = function () {
			//	$scope.$root.docTitle('Users | Analytics | ' + profile.Name, 10);
			//};
			//
			//if (profile.ID > 0) {
			//	init();
			//}
			//else {
			//	var watcher = $scope.$watch('$root.activeProfile', function (val) {
			//		if (val.ID > 0) {
			//			profile = val;
			//			watcher();
			//			init();
			//		}
			//	})
			//}

		}]);

})();
